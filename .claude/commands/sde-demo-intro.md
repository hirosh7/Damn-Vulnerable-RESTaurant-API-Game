---
description: Run the SDE MCP live demo opening segment (Steps 0-1.5 of setup-security-plan-from-repo)
---

Read `SDE-DEMO-INTRO.md` in the repository root and execute it exactly as written:

1. Verify the SD Elements MCP connection (Step 0).
2. Ask the user the Step 1 interactive questions live, one at a time, waiting for real
   answers each time: repository, project mode, business unit, application, project name
   (following the naming rule — no "teaser"/internal-only wording), and risk policy.
3. Create and validate the SD Elements project (Step 1.5).
4. Stop per the "STOP HERE" section — do not continue into Step 1.7 or any later step.

This is a live-demo opening segment, not the full `setup-security-plan-from-repo` workflow.
